# Scripts

## Template sanity check
Verifies base template + overrides merge behavior.

**Run:**
```bash
npx tsx scripts/template-merge-check.ts
```

**Expected:**
- Prints merged template JSON
- Confirms overrides apply without affecting base template defaults
